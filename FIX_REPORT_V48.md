# AutoDrive v48 — Notifications Fix Report

## إصلاحات الإشعارات
1. إضافة قناة FCM الافتراضية في AndroidManifest حتى إشعارات notification-payload تظهر عندما يكون التطبيق بالخلفية/مغلقاً.
2. رفع FCM token فور اكتمال تسجيل مستخدم جديد؛ سابقاً قد ينتظر حتى إعادة تشغيل التطبيق.
3. مزامنة جدول notifications عند دخول Home، وليس فقط بعد فتح شاشة الإشعارات.
4. إعادة مزامنة notifications عند Refresh من Home.
5. loadUnreadCount أصبح يجلب من السيرفر ثم يترك Flow يحدث العداد تلقائياً.

## النتيجة المتوقعة
- الجرس وعدد غير المقروء يتحدثان عند دخول الرئيسية.
- مستخدم جديد يصبح قابلاً لاستقبال Push مباشرة بعد التسجيل.
- إشعارات FCM بالخلفية تستخدم قناة autodrive_general عالية الأهمية.

## ملاحظة فحص
لم يتم تنفيذ Gradle build في هذه البيئة إذا كانت Gradle distribution غير متوفرة محلياً؛ التعديلات محدودة لمسارات Kotlin/Manifest المذكورة.

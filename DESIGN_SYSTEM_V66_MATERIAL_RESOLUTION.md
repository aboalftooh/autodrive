# DESIGN_SYSTEM_V66_MATERIAL_RESOLUTION

All six historical Material candidates were source-reviewed and resolved in the v66 analysis layer as `VERIFIED_ALLOWED_PRIMITIVE`. Ratchet commitment is deferred because runtime finality is blocked.

- `DS59-MATC-FC49FB269C` — `CircularProgressIndicator` in `app/src/main/kotlin/com/autodrive/app/feature/home/presentation/HomeHeroComponents.kt` — **VERIFIED_ALLOWED_PRIMITIVE**. Inline/loading-state progress has no project-global DS equivalent; replacing it with a button/dialog/pattern would change state, slot, layout, or interaction semantics.
- `DS59-MATC-EA81ACB322` — `CircularProgressIndicator` in `app/src/main/kotlin/com/autodrive/app/feature/reports/presentation/log/CompetitionHistoryScreen.kt` — **VERIFIED_ALLOWED_PRIMITIVE**. Inline/loading-state progress has no project-global DS equivalent; replacing it with a button/dialog/pattern would change state, slot, layout, or interaction semantics.
- `DS59-MATC-B28DBA9BD1` — `CircularProgressIndicator` in `app/src/main/kotlin/com/autodrive/app/feature/reports/presentation/log/CompetitionHistoryScreen.kt` — **VERIFIED_ALLOWED_PRIMITIVE**. Inline/loading-state progress has no project-global DS equivalent; replacing it with a button/dialog/pattern would change state, slot, layout, or interaction semantics.
- `DS59-MATC-9B9E4A62B6` — `TopAppBar` in `app/src/main/kotlin/com/autodrive/app/feature/reports/presentation/log/InvoiceDetailScreen.kt` — **VERIFIED_ALLOWED_PRIMITIVE**. Screen-scaffold top bar has no global replacement in the authoritative mapping; AutoDrive ScreenHeader is not API/slot-equivalent to TopAppBar with dynamic invoice title and navigationIcon.
- `DS59-MATC-353C4BA208` — `CircularProgressIndicator` in `app/src/main/kotlin/com/autodrive/app/feature/reports/presentation/log/InvoiceDetailScreen.kt` — **VERIFIED_ALLOWED_PRIMITIVE**. Inline/loading-state progress has no project-global DS equivalent; replacing it with a button/dialog/pattern would change state, slot, layout, or interaction semantics.
- `DS59-MATC-D44DE38681` — `CircularProgressIndicator` in `app/src/main/kotlin/com/autodrive/app/feature/reports/presentation/log/WinWeeksScreen.kt` — **VERIFIED_ALLOWED_PRIMITIVE**. Inline/loading-state progress has no project-global DS equivalent; replacing it with a button/dialog/pattern would change state, slot, layout, or interaction semantics.
